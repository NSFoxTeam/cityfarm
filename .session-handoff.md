# Session Handoff — 2026-03-05

## Summary
Implemented full plan: pH/TDS/EC sensors + firmware Go→Rust rewrite + backend API + frontend dashboard. Committed and pushed to main (685308e). 70 files, ~13K lines.

## What Was Done

### Firmware (Rust) — 17 source files
- Deleted Go stubs, created Rust project with cargo
- Sensors: DS18B20, ADS1115 (shared), pH (Nernst compensation), TDS/EC, moisture, DHT22, BH1750
- Relay: GPIO17 active LOW, safety cutoff + cooldown
- Transport: SQLite FIFO buffer + HTTP POST with retry
- CLI: `run`, `calibrate ph`, `test-sensors`
- Cross-compile: `.cargo/config.toml` for aarch64

### Backend (Go) — 7 files
- TimescaleDB migration (hypertable)
- Models + validation for 8 sensor types
- Store: pgx batch insert, GetLatest, GetHistory
- Alerting: thresholds for pH/TDS/EC/solution_temp
- API: POST/GET readings, X-API-Key auth, Chi router

### Frontend (React) — 16 files
- React 19 + Vite + Tailwind 4 + shadcn/ui
- WaterQuality: pH (purple), TDS (cyan), EC (teal), solution_temp (rose)
- EnvironmentSection: temp, humidity, light, moisture
- Recharts dual Y-axis, AlertBanner, TanStack Query 10s polling
- Build clean: 673KB JS, 20KB CSS

### Config
- CLAUDE.md: firmware→Rust, added new sensors, updated commands
- rpi-engineer.md: Rust crates, pH/TDS formulas, pinout

## Hardware Status (Ordered)
- DFRobot pH V2 (SEN0161-V2) — ~4966₽
- DFRobot TDS (SEN0244) — ~1244₽
- Buffer solutions pH 4.01/6.86/9.18 — ~593₽
- Sonoff S60TPF WiFi plug (user noted S26R2ZB Zigbee would be better)
- Sonoff Dongle Max (Zigbee/Thread PoE)
- DS18B20 — already available

## Pending / Next Steps
1. Wire sensors when they arrive: pH→ADS1115 A1, TDS→A2, DS18B20→GPIO22
2. RPi: `dtoverlay=w1-gpio,gpiopin=22` in /boot/firmware/config.txt
3. Cross-compile: `cross build --release --target aarch64-unknown-linux-gnu`
4. Calibrate pH with buffer solutions
5. Set up Zigbee2MQTT + Dongle Max for smart plugs (light, fans)
6. Camera integration: 3x Xiaomi MJSXJ09CM — RTSP via NAS mode or python-miio
7. YOLO plant health model — start with pre-trained from Roboflow

## Key Decisions
- Firmware language: Rust (not Go) — type safety for embedded, embedded-hal traits
- Zigbee preferred over WiFi for smart plugs (Sonoff S26R2ZB > S60TPF)
- pH/TDS on ADS1115 channels A1/A2 (A0 = moisture, A3 = free)
- DS18B20 on GPIO22 (1-Wire sysfs, not I2C)

## Notes
- cargo check fails on macOS (expected — rppal/linux-embedded-hal are Linux-only)
- 3 cameras = 1 per shelf level, ideal for per-tray YOLO monitoring
- Atmeex AirNanny A7 has no Zigbee — needs IR bridge or smart plug on/off
