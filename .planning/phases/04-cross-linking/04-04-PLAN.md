---
phase: 04-cross-linking
plan: 04
type: execute
wave: 2
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - CityFarm/01-Основы/*.md
  - CityFarm/02-Системы-выращивания/*.md
  - CityFarm/03-Питательные-растворы/*.md
  - CityFarm/04-Культуры/*.md
  - CityFarm/05-Микроклимат/*.md
  - CityFarm/06-Освещение/*.md
  - CityFarm/07-Проектирование/*.md
  - CityFarm/08-Помещения/*.md
  - CityFarm/09-Устойчивое-развитие/*.md
autonomous: true
requirements: [LINK-02]
must_haves:
  truths:
    - "Every content note has at least one домен/* tag"
    - "Every content note has a тип/* tag matching its type: frontmatter field"
    - "Cross-cutting topic tags (iot, pH, EC, LED, etc.) are preserved where relevant"
    - "Redundant tags that duplicate note title or folder name are removed"
    - "Total unique tags reduced from 267 to approximately 50-70"
  artifacts:
    - path: "CityFarm/01-Основы/*.md"
      provides: "6 notes with normalized tags"
    - path: "CityFarm/02-Системы-выращивания/*.md"
      provides: "7 notes with normalized tags"
    - path: "CityFarm/03-Питательные-растворы/*.md"
      provides: "22 notes with normalized tags"
    - path: "CityFarm/04-Культуры/*.md"
      provides: "13 notes with normalized tags"
    - path: "CityFarm/05-Микроклимат/*.md"
      provides: "7 notes with normalized tags"
    - path: "CityFarm/06-Освещение/*.md"
      provides: "19 notes with normalized tags"
    - path: "CityFarm/07-Проектирование/*.md"
      provides: "6 notes with normalized tags"
    - path: "CityFarm/08-Помещения/*.md"
      provides: "9 notes with normalized tags"
    - path: "CityFarm/09-Устойчивое-развитие/*.md"
      provides: "6 notes with normalized tags"
  key_links:
    - from: "all content notes"
      to: "tag taxonomy"
      via: "YAML frontmatter tags: array"
      pattern: "домен/"
---

<objective>
Normalize the tag system across all 95 content notes using a structured 3-tier taxonomy: domain tags, content type tags, and cross-cutting topic tags.

Purpose: Replace 267 ad-hoc tags with a consistent taxonomy (~50-70 unique tags) that enables meaningful filtering and discovery in Obsidian. Domain tags map to folder topics, type tags map to note type, and cross-cutting tags preserve useful search terms.
Output: All 95 content notes with consistent, taxonomy-compliant tags.
</objective>

<execution_context>
@/Users/nsfox/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nsfox/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cross-linking/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define canonical tag list and normalize tags on folders 01-05 (55 notes)</name>
  <files>CityFarm/01-Основы/*.md, CityFarm/02-Системы-выращивания/*.md, CityFarm/03-Питательные-растворы/*.md, CityFarm/04-Культуры/*.md, CityFarm/05-Микроклимат/*.md</files>
  <action>
**Step 1: Build canonical tag list** based on RESEARCH.md taxonomy:

**Tier 1 — Domain tags (one per note, matching folder):**
- `домен/основы`, `домен/системы`, `домен/растворы`, `домен/культуры`, `домен/микроклимат`, `домен/освещение`, `домен/проектирование`, `домен/помещения`, `домен/устойчивость`

**Tier 2 — Content type tags (one per note, matching `type:` frontmatter):**
- `тип/знание` (type: knowledge), `тип/параметр` (type: parameter), `тип/справочник` (type: reference), `тип/практика` (type: knowledge with workflow content)

**Tier 3 — Cross-cutting topic tags (0-3 per note, only where genuinely useful for search):**
- Technical terms: `iot`, `pH`, `EC`, `LED`, `PPFD`, `DLI`
- System types: `DWC`, `NFT`, `аэропоника`, `капельный-полив`
- Crop categories: `салаты`, `микрозелень`, `травы`
- Topics: `фотосинтез`, `спектр`, `калибровка`, `диагностика`, `субстраты`, `водоподготовка`

**Step 2: Process each of the 55 notes in folders 01-05:**
1. Read current `tags:` array from frontmatter
2. Replace with taxonomy-compliant set:
   - Add 1 domain tag (based on folder)
   - Add 1 type tag (based on `type:` field in same frontmatter)
   - Keep 0-3 cross-cutting tags from Tier 3 list IF they appear in current tags and are genuinely useful
   - Drop tags that duplicate note title, folder name, or add no findability
3. Write updated frontmatter

**Rules:**
- Only modify the `tags:` field in YAML frontmatter. Do NOT change any other frontmatter fields or body content.
- Use `тип/практика` for notes that describe step-by-step procedures (pH meter calibration, EC measurement algorithm, etc.) even if `type: knowledge` in frontmatter.
- Maximum 5 tags per note (1 domain + 1 type + up to 3 cross-cutting).
  </action>
  <verify>
    <automated>cd /Users/nsfox/Development/cityfarm && for f in 01-Основы 02-Системы-выращивания 03-Питательные-растворы 04-Культуры 05-Микроклимат; do count=$(grep -r "домен/" "CityFarm/$f/" --include="*.md" -l | wc -l | tr -d ' '); total=$(find "CityFarm/$f/" -name "*.md" -not -name "*MOC*" | wc -l | tr -d ' '); echo "$f: $count/$total have domain tag"; done</automated>
  </verify>
  <done>All 55 content notes in folders 01-05 have exactly 1 домен/* tag and 1 тип/* tag. Cross-cutting tags are preserved where useful. No note has more than 5 tags.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize tags on folders 06-09 (40 notes) and validate full taxonomy</name>
  <files>CityFarm/06-Освещение/*.md, CityFarm/07-Проектирование/*.md, CityFarm/08-Помещения/*.md, CityFarm/09-Устойчивое-развитие/*.md</files>
  <action>
**Step 1: Process each of the 40 notes in folders 06-09:**
Same process as Task 1 — replace tags with taxonomy-compliant set using the canonical tag list defined in Task 1.

**Folder-specific notes:**
- 06-Освещение: many notes will get cross-cutting tags like `LED`, `PPFD`, `DLI`, `спектр`, `фотосинтез`
- 07-Проектирование: practical assembly notes get `тип/практика`
- 08-Помещения: infrastructure notes, some with `iot` tag
- 09-Устойчивое-развитие: resource efficiency topics

**Step 2: Validate across entire vault (all 95 notes):**
1. Extract all tags from all 95 content notes
2. Count unique tags — target 50-70
3. Verify every note has exactly 1 `домен/*` tag
4. Verify every note has exactly 1 `тип/*` tag
5. Check no orphan tags exist (tags appearing on only 1 note that aren't genuinely useful)
6. Report final tag distribution

**Rules:**
- Only modify `tags:` in YAML frontmatter
- Maximum 5 tags per note
- Cross-cutting tags from Tier 3 canonical list only
  </action>
  <verify>
    <automated>cd /Users/nsfox/Development/cityfarm && echo "=== Domain tag coverage ===" && grep -rl "домен/" CityFarm/ --include="*.md" | grep -v MOC | grep -v Home | grep -v Glossary | grep -v _templates | wc -l && echo "=== Unique tags ===" && grep -h "^  - " CityFarm/0*/*.md 2>/dev/null | sort -u | wc -l</automated>
  </verify>
  <done>All 95 content notes have taxonomy-compliant tags. Total unique tags between 50-70 (down from 267). Every note has 1 domain tag and 1 type tag. No orphan or redundant tags remain.</done>
</task>

</tasks>

<verification>
- All 95 content notes have exactly 1 `домен/*` tag
- All 95 content notes have exactly 1 `тип/*` tag
- Total unique tags reduced from 267 to 50-70 range
- No note exceeds 5 tags
- Cross-cutting tags are consistent (same tag used across all relevant notes)
- No frontmatter fields other than `tags:` were modified
</verification>

<success_criteria>
- 95 notes with normalized, taxonomy-compliant tags
- Unique tag count reduced to 50-70 from 267
- Domain and type tag coverage at 100%
- Tag system enables meaningful Dataview queries and Obsidian tag filtering
</success_criteria>

<output>
After completion, create `.planning/phases/04-cross-linking/04-04-SUMMARY.md`
</output>
